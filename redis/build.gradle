dependencies {
    api project(':base')
    api group: "redis.clients", name: "jedis", version: "4.4.3"

    testImplementation testFixtures(project(':base'))
}

test {
    systemProperty "fraho.jwt.refresh.redis.host", System.getProperty("fraho.jwt.refresh.redis.host")
}
