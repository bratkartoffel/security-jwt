dependencies {
    compile('org.springframework.boot:spring-boot-starter-web')
    compile('org.springframework.boot:spring-boot-starter-security')
    compile("com.fasterxml.jackson.datatype:jackson-datatype-jdk8")

    compile("com.nimbusds:nimbus-jose-jwt:${nimbusJwtVersion}")
    compile("commons-codec:commons-codec:${commonsCodecVersion}")
    optional("io.swagger:swagger-annotations:${swaggerAnnotations2Version}")

    optional("org.bouncycastle:bcprov-jdk15on:${bouncyCastleVersion}")

    testCompile("org.powermock:powermock-api-mockito2:${powerMockVersion}")
    testCompile("org.powermock:powermock-module-junit4:${powerMockVersion}")
}

compileModuleInfoJava {
    inputs.property("moduleName", "fraho.securityJwt.base")
}
