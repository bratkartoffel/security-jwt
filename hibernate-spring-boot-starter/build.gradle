dependencies {
    compile project(':base-spring-boot-starter')
    compile project(':hibernate')

    testCompile project(path: ':base', configuration: 'testArchives')
    testCompile project(path: ':hibernate', configuration: 'testArchives')
}
