dependencies {
    compile project(':base')
    testCompile project(path: ':base', configuration: 'testArchives')

    compile("net.jodah:expiringmap:${expiringMapVersion}")
}

compileModuleInfoJava {
    inputs.property("moduleName", "fraho.securityJwt.internal")
}
