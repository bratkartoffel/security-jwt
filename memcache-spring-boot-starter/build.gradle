dependencies {
    api project(':base-spring-boot-starter')
    api project(':memcache')

    testImplementation testFixtures(project(':base'))
    testImplementation testFixtures(project(':memcache'))
}
